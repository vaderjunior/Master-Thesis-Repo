name: sq3_r4_fb
description: >
  SQ3 round 4, feedback arm, the final round. KB e4ff5e0234d51ba3: base 360 records
  plus 60 corrections. Round 4 adds 12, being every item the ROUND-3 FEEDBACK
  ARM got wrong in b4.

  On the fixed 350-item stratum (held_out + pool, never corrected in any
  round) the effect is constant rather than growing: fb - ctl macro +0.012,
  +0.011, +0.016 across rounds 1-3 at pool sizes 20, 33 and 48. Complete
  separation so far, min(fb) 0.368 > max(ctl) 0.363, exact one-sided rank test
  p = 1/20 on 3 v 3. A fourth paired comparison in the same direction gives
  p = 1/70.

  This round also closes the batch pool: all four batches are now corrected,
  so the fixed stratum remains the only never-corrected measurement and the
  `clean` stratum ceases to add anything.
records_path: kb/records_sq3_fb_r4.jsonl
chroma_path: kb/chroma_sq3_fb_r4
subset: en_dev_eval_sq3_types
arms: [rag]
provider: peasec
pinned_model: Qwen/Qwen3-VL-32B-Instruct
temperature: 1.0
allow_fallback: false
n_votes: 3
workers: 8
gate_mapping: strict
retrieval:
  strategy: hybrid
  k_definitions: 2
  k_guidelines: 2
  k_examples: 5
  k_examples_feedback: 1
  use_mmr: true
  mmr_lambda: 0.7
  mmr_pool_factor: 3
  use_reranker: false
  rrf_k: 60